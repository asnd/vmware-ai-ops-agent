# Secrets referenced by *_env fields in config.yaml.
LLM_GATEWAY_API_KEY=sk-replace-me
# Optional inbound auth for this service's own HTTP API.
# AIGW_API_KEY=local-dev-key
# Optional credentials for the upstream source / downstream sink.
# SOURCE_API_TOKEN=
# SINK_API_TOKEN=
